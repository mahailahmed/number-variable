// Declare a number variable
var age = 25;
// Perform arithmetic operations
var x = 10;
var y = 5;
var sum = x + y;
var difference = x - y;
var product = x * y;
var quotient = x / y;
// Print the results
console.log(age); //output is    (25)
console.log(sum); //output is    (15)
console.log(difference); //output is     (5)
console.log(product); //output is    (50)
console.log(quotient); //output is    (2)
